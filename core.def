# Core Concepts
#
# These are the core philosophical constructs required to create any universe or have any action.

Consideration is. # a persistent creation

ViewPoint is a Consideration.
DimensionPoint is a Consideration.
Location is a Consideration. # keeping Location abstract for now

# DimensionPoint has a Location named location.

ViewPoint can create DimensionPoint.
DimensionPoint has a ViewPoint named owner.
# TODO: DimensionPoint must have owner.
# TODO: ViewPoint has many DimensionPoint: particles.

# TODO: Form is many DimensionPoint.

# Compiler Concepts
#
# These are concepts that translate into something the compiler does or
# knows about. At some point we may get around to defining these
# conceptually (because they can be) but that doesn't seem like a good
# use of our time at the moment.

Number is. # can be any form of a number (integer, decimal, float, whatever)
Number has a Number named value.
String is. # A unicode string.
String has a String named value.
