"""Generated by the Define compiler."""

from typing import ClassVar, final

from define.runtime import literal

import local.my_domain_com.my_lib.input
import local.my_domain_com.my_lib.item


class Other(literal.Action):
    implied_qualities: ClassVar[tuple[type[literal.Quality], ...]] = (
        local.my_domain_com.my_lib.input.Input,
    )

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition(
                    "position<trigger_pos>",
                    scheduler=on_particle.scheduler,
                ),
                literal.LocalPosition(
                    "position<output>",
                    constraints=(
                        local.my_domain_com.my_lib.item.Item,
                    ),
                    scheduler=on_particle.scheduler,
                ),
            ],
        )


@final
class OtherGuarantees:
    def __init__(self):
        self.global_position_input = literal.Guarantee()
        self.position_output = literal.Guarantee()


@final
class OtherExecution:
    def __init__(
        self,
        action: Other,
        scheduler: literal.Scheduler,
        *,
        destruction_connections: literal.DestructionConnections | None = None,
    ):
        self.action = action
        self.scheduler = scheduler
        self.guarantees = OtherGuarantees()
        self.destruction_connections = destruction_connections
        self.destruction_position_position_output__global_position_item: literal.Position
        self.join_for_move_global_position_input_to_position_output: literal.Join
        self.join_for_empty_rule_global_position_input: literal.Join

    def accept_for_empty_rule_global_position_input(self):
        if not self.join_for_empty_rule_global_position_input.arrive():
            return
        self.move_global_position_input_to_position_output()

    def move_global_position_input_to_position_output(self):
        if not self.join_for_move_global_position_input_to_position_output.arrive():
            return
        self.action.on_particle.get_position(
            local.my_domain_com.my_lib.input.Input
        ).move_particle_to(
            self.action.get_interface_position(
                "position<output>"
            )
        )
        self.destruction_position_position_output__global_position_item = self.action.get_interface_position(
            "position<output>"
        ).particle.get_position(
            local.my_domain_com.my_lib.item.Item
        )
        self.guarantees.global_position_input.publish(
            self.scheduler,
            self.destroy_position_output__global_position_item,
        )

    def destroy_position_output__global_position_item(self):
        literal.continue_destruction(self.continue_destroy_position_output__global_position_item)

    def continue_destroy_position_output__global_position_item(self):
        self.destruction_position_position_output__global_position_item.destroy_particle()
        self.destroy_position_output()

    def destroy_position_output(self):
        literal.continue_destruction(self.continue_destroy_position_output)

    def continue_destroy_position_output(self):
        self.action.get_interface_position(
            "position<output>"
        ).destroy_particle()
        self.guarantees.position_output.publish(
            self.scheduler,
        )
