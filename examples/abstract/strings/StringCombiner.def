it is a Machine.
# This exposes it to this entire namespace, including all sub-namespaces.
# TODO: What if we only want to expose it to exactly examples?
it is exposed to examples.

# This defines a property.
# TODO: The full namespace syntax here doesn't read well.
it has a examples/abstract/strings/TwoStrings named myStrings {
    that is exposed for creation.
    that must be set on creation. # TODO: Make this the default if not specified?
    that must not be changed after creation. # TODO: Make this be the default.
}

# Note that for certain types,all programs automatically have access to them, such as
# Number, String, Boolean, Integer, etc. These are "core" types. They must not be referred
# to like "core/String."
it has a String named combination {
    that is not exposed for creation.
    # A "this is set" clause indicates this is the only way to set this.
    that is set {
        by doing {
            combination = container.stringOne append container.stringTwo.
        }
    }
    that is cached.
}